{
  "schemaVersion": 1,
  "objective": "Make Claude/Codex switching crash-resistant through an atomic checkpoint and a deterministic, model-neutral scorecard.",
  "phase": "agent-checkpoint-and-scorecard",
  "status": "complete",
  "productionBaseline": {
    "endpoint": "https://forleads.vercel.app/api/health",
    "reportedAt": "2026-06-25T22:53:37.000Z",
    "expected": {
      "ok": true,
      "productionPolicy": {
        "mockConnectorWritesAllowed": false,
        "liveModeViolations": []
      },
      "modes": {
        "persistence": "supabase",
        "geocoder": "nominatim",
        "property": "osm",
        "imagery": "mapillary",
        "agent": "live"
      }
    }
  },
  "acceptanceCriteria": [
    "A checkpoint is written atomically and captures objective, phase, repository state, production baseline, scorecard state, and exact resume command.",
    "The scorecard records each check as pending, running, passed, failed, timed out, or interrupted so a crash cannot masquerade as success.",
    "Production policy is verified live: mock connector writes are forbidden, there are zero live-mode violations, and all five core modes are live.",
    "Typecheck, lint, and tests pass from one model-neutral command.",
    "SESSION_HANDOFF.md is generated from machine state instead of relying on agent memory."
  ],
  "nextActions": [
    "Start the next phase by replacing the objective, phase, acceptance criteria, and next actions in session-state.json.",
    "Investigate the production address autocomplete no-match behavior visible in the June 25 Clarksburg screenshots as its own evidence-backed phase.",
    "Run npm run agent:scorecard before the next handoff."
  ],
  "notes": [
    "Do not overwrite or discard unrelated uncommitted product work.",
    "The local production build is intentionally not a scorecard gate because this machine has a documented maplibre/webpack memory stall; Vercel owns the production-build gate.",
    "This phase passed the model-neutral scorecard at 100/100 with all 87 tests passing."
  ]
}
